version: '3.9'

services:
  postgres:
    image: postgres:16-alpine
    container_name: bar_booking_postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: bar_booking_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d bar_booking_db"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
